server:
  port: ${PORT}
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcioasm84/ead-config-server-repo
          default-label: main
          #username: ${GIT_USERNAME}
          #password: ${GIT_PASSWORD}
          
encrypt:
  key: MFswDQYJKoZIhvcNAQEBBQADSgAwRwJAdooMf+8+f+g1C6SHi0tvbd1jC1htqMAv
ead:
  configserver:
    username: ${CONFIG_SERVER_USERNAME}
    password: ${CONFIG_SERVER_PASSWORD}
        
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}
  instance:
    hostname: ${APP_DOMAIN_NAME}
    non-secure-port: 80
